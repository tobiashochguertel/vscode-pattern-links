{
  "name": "pattern-links",
  "displayName": "Link Patterns",
  "description": "Automatically turn text into links based upon regex patterns.",
  "version": "1.0.2",
  "publisher": "TobiasHochgurtel",
  "author": {
    "name": "Tobias Hochgürtel",
    "url": "https://github.com/TobiasHochgurtel"
  },
  "contributors": [
    {
      "name": "Dan Levett",
      "url": "https://github.com/dlevs",
      "description": "Original author"
    }
  ],
  "license": "MIT",
  "engines": {
    "vscode": "^1.85.0",
    "node": ">=18.0.0"
  },
  "repository": {
    "url": "https://github.com/TobiasHochgurtel/vscode-pattern-links"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "icon": "assets/icon.png",
  "keywords": [
    "link",
    "pattern",
    "regex",
    "auto",
    "autolink"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "type": "object",
      "title": "Pattern Links",
      "properties": {
        "patternlinks.rules": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "linkPattern",
              "linkTarget"
            ],
            "properties": {
              "linkPattern": {
                "type": "string",
                "markdownDescription": "A regular expression used to find text in the document to turn into links.\n\nExample: `ISSUES-\\d+`\n\nThe example pattern would match text like `ISSUE-1` and `ISSUE-200`.",
                "default": ""
              },
              "linkPatternFlags": {
                "type": "string",
                "markdownDescription": "A list of [regular expression flags](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Regular_Expressions#advanced_searching_with_flags) to use.\n\nExample: `is`\n\nThe example flags make the search case-insensitive (`i` flag) and allows the `.` character to match over multiple lines (`s` flag). The `g` flag is always applied.",
                "default": ""
              },
              "linkTarget": {
                "type": "string",
                "markdownDescription": "The link template.\n\nExample: `https://myorg.atlassian.net/browse/$0`\n\nIn the example, `$0` will be replaced with the captured text from the linkPattern; the final URL might be `https://myorg.atlassian.net/browse/ISSUE-200`.",
                "default": ""
              },
              "languages": {
                "type": "array",
                "markdownDescription": "An array of languages to enable this rule for.\n\nExample: `[\"plaintext\", \"markdown\"]`\n\nIf no languages are specified, the rule is enabled for all languages.",
                "items": {
                  "type": "string"
                },
                "default": []
              }
            },
            "additionalProperties": false
          },
          "default": [],
          "markdownDescription": "A list of rules for turning text into clickable links."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "pnpm run compile && pnpm run lint && pnpm run test",
    "compile": "tsc -p ./",
    "lint": "eslint . --ext .ts,.tsx",
    "watch": "tsc -watch -p ./",
    "test": "node ./node_modules/@vscode/test-electron/out/runTest.js --extensionDevelopmentPath=./ --extensionTestsPath=./out/test/suite",
    "package:check": "pnpm pack --dry-run",
    "package:size": "pnpm pack --dry-run 2>&1 | grep -A 1 \"package size\" || true",
    "package:list": "pnpm pack --dry-run 2>&1 | grep \"^[^D].*\\.js\\|.*\\.json\\|.*\\.d\\.ts\\|.*\\.md\" || true",
    "package:analyze": "./scripts/analyze-package.sh",
    "prepack": "pnpm run package:size && pnpm run package:list",
    "publish:vscode": "vsce publish",
    "publish:ovsx": "ovsx publish",
    "publish:all": "pnpm run publish:vscode && pnpm run publish:ovsx"
  },
  "devDependencies": {
    "@types/glob": "^8.1.0",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.11.16",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "@vscode/test-electron": "^2.4.1",
    "@vscode/vsce": "^3.2.1",
    "eslint": "^9.0.0-alpha.0",
    "eslint-config-prettier": "^9.1.0",
    "glob": "^10.3.10",
    "mocha": "^10.2.0",
    "ovsx": "^0.10.1",
    "prettier": "^3.2.5",
    "type-fest": "^4.10.2",
    "typescript": "^5.3.3"
  }
}
